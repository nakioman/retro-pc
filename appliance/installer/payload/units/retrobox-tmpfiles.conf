# systemd-tmpfiles: runtime directory for the RetroBox daemon.
# Installed to /etc/tmpfiles.d/retrobox.conf on the target.
# /run is tmpfs, so this is recreated on every boot with the right owner.
d /run/retrobox 0755 retrobox retrobox -
