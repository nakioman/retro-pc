# Optional prefix compiled into requests to the RetroBox API.
# Keep this empty when Vite and the API share an origin (the production default).
VITE_API_BASE_URL=

# Development-only target for Vite's /api proxy.
VITE_API_PROXY_TARGET=http://localhost:8080
